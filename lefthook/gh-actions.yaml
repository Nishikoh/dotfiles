lint: &lint
  parallel: true
  skip:
    - merge
    - rebase
  commands:
    actionlint:
      tags: lint build
      glob: &gh_file ".github/workflows/*"
      run: actionlint {staged_files}
      # TODO: permission, timeout, etc.

pre-commit: *lint
